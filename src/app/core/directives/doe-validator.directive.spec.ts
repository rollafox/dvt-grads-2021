import { DoeValidatorDirective } from './doe-validator.directive';

describe('DoeValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new DoeValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
